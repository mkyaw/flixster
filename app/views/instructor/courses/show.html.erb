<br />
<div class="booyah-box col-xs-10 col-xs-offset-1">
  <h1 class="text-center"><%= @course.title %></h1>
  <br />
  <h3 class="text-center"><%= @course.description %></h3>
  <br />
  <h3 class="text-center"><%= number_to_currency @course.cost %></h3>
  <br />

  <h4>
    <ol class="col-xs-11 col-xs-offset-1">
      <% @course.sections.each do |section| %>
        <li>
          <%= section.title %>
          <%= link_to "New Lesson", new_instructor_section_lesson_path(section), :class => "pull-right btn btn-primary btn-xs" %>
          <ol>
            <% section.lessons.each do |lesson| %>
              <br />
              <li><b><%= lesson.title %></b> - <%= lesson.subtitle %></li>
            <% end %>
          </ol>
          <div class="clr"></div>
          <br />
        </li>
        <hr />
      <% end %>
    </ol>
  </h4>

  <br />

  <div class="text-center">
    <%= link_to "Preview as a Student", course_path(@course), :class => "btn btn-success" %>
    <br />
    <br />
    <%= link_to "Add a new section...", new_instructor_course_section_path(@course), :class => "btn btn-primary btn-lg" %>
  </div>
  <div class="clr"></div>
  <br />

</div>